<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <link rel="stylesheet" href="/css/style.css">
    <title>Football Apparels App</title>
</head>
<body>
  <%- include ('partials/navbar') %>
  
  <div class="container mt-5 pt-5">
    <div class="text-center mb-4"><h2>Product Details</h2></div>
    <% if (product) { %> <!-- Check if product data exists -->
      <div class="single-product card mb-4">
        <div class="row g-0">
          <div class="col-md-4">
          <img src="/images/<%= product.image %>" class="card-img-top" alt="<%= product.name %>">
          </div>
          <div class="col-md-8">
            <div class="card-body single-product-details">
              <h3 class="card-title"><%= product.name %></h3>
              <p class="card-text"><%= product.description %></p>
              <p class="card-text"><strong>Price: $<%= product.price %></strong></p>
              <a href="/products" class="btn btn-primary">Back</a>
            </div>
          </div>
        </div>
      </div>
    <% } else { %>
      <p>No product found.</p>
    <% } %>
  </div>
</body>
</html>
